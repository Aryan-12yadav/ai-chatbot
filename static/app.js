const chat=document.getElementById('chat'); const form=document.getElementById('form'); const input=document.getElementById('message'); const status=document.getElementById('status'); const session='demo-'+Date.now();
function add(role,text,sources=[]){const d=document.createElement('div');d.className='msg '+role;d.textContent=text;if(sources.length){const s=document.createElement('div');s.className='sources';s.textContent='Sources: '+sources.join(', ');d.appendChild(s)}chat.appendChild(d);chat.scrollTop=chat.scrollHeight}
fetch('/health').then(r=>r.json()).then(x=>{status.textContent=x.llm_configured?'API ready':'API key missing'}).catch(()=>status.textContent='Offline');
form.addEventListener('submit',async e=>{e.preventDefault();const message=input.value.trim();if(!message)return;add('user',message);input.value='';const waiting=document.createElement('div');waiting.className='msg assistant';waiting.textContent='Thinking...';chat.appendChild(waiting);try{const r=await fetch('/api/chat',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({message,session_id:session})});const x=await r.json();waiting.remove();if(!r.ok)throw new Error(x.detail||'Request failed');add('assistant',x.response,x.sources||[])}catch(err){waiting.remove();add('assistant','Error: '+err.message)}});
